only avalaible on MAC OSX, this script will install homebrew and install basic applications. Useful when your hardrive burn without reason ..

Before running the script, you need :
  - command line tools osX
